import React from 'react';
import useAuth from './useAuth';
import { useParams } from 'react-router-dom';

const CheckAuthUserId = ({ auth, formData }) => {
  
const checkAuth = () =>{
    auth
}
  return checkAuth()
  
};

export default CheckAuthUserId;
